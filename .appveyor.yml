image: Visual Studio 2017
clone_depth: 1
configuration: Release
platform: AnyCPU
before_build:
  - cmd: dotnet --version
build_script:
  - dotnet clean BuildTest\BuildTest.csproj -c Release
  - dotnet publish BuildTest\BuildTest.csproj -c Release -v Normal
